// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: pocket/service/params.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Coin } from "../../cosmos/base/v1beta1/coin";

export const protobufPackage = "pocket.service";

/** Params defines the parameters for the module. */
export interface Params {
  /**
   * The amount of uPOKT required to add a new service.
   * This will be deducted from the signer's account balance,
   * and transferred to the pocket network foundation.
   */
  addServiceFee:
    | Coin
    | undefined;
  /**
   * target_num_relays is the target for the EMA of the number of relays per session.
   * Per service, onchain relay mining difficulty will be adjusted to maintain this target.
   */
  targetNumRelays: number;
}

function createBaseParams(): Params {
  return { addServiceFee: undefined, targetNumRelays: 0 };
}

export const Params: MessageFns<Params> = {
  encode(message: Params, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.addServiceFee !== undefined) {
      Coin.encode(message.addServiceFee, writer.uint32(10).fork()).join();
    }
    if (message.targetNumRelays !== 0) {
      writer.uint32(16).uint64(message.targetNumRelays);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Params {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.addServiceFee = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.targetNumRelays = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      addServiceFee: isSet(object.addServiceFee) ? Coin.fromJSON(object.addServiceFee) : undefined,
      targetNumRelays: isSet(object.targetNumRelays) ? globalThis.Number(object.targetNumRelays) : 0,
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.addServiceFee !== undefined) {
      obj.addServiceFee = Coin.toJSON(message.addServiceFee);
    }
    if (message.targetNumRelays !== 0) {
      obj.targetNumRelays = Math.round(message.targetNumRelays);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Params>, I>>(base?: I): Params {
    return Params.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Params>, I>>(object: I): Params {
    const message = createBaseParams();
    message.addServiceFee = (object.addServiceFee !== undefined && object.addServiceFee !== null)
      ? Coin.fromPartial(object.addServiceFee)
      : undefined;
    message.targetNumRelays = object.targetNumRelays ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
