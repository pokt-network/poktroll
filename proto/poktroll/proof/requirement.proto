syntax = "proto3";

package poktroll.proof;

option go_package = "github.com/pokt-network/poktroll/proto/types/proof";

enum ProofRequirementReason {
  NOT_REQUIRED = 0;
  PROBABILISTIC = 1;
  THRESHOLD = 2;
}